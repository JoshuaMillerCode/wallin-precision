{
  "mcpServers": {
    "mongodb": {
      "command": "npx",
      "args": [
        "-y",
        "mongodb-mcp-server@latest"
      ],
      "env": {
        "MDB_MCP_CONNECTION_STRING": "${env:MONGODB_ATLAS_URI}",
        "MDB_MCP_READ_ONLY": "false"
      }
    }
  }
}
